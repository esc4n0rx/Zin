variavel x = 0;

enquanto (x < 5) {
    escreva("Valor de x: " + x);

    se (x % 2 == 0) {
        escreva("x é par.");
    } senao {
        escreva("x é ímpar.");
    }

    calcule(x = x + 1);
}

escreva("Fim do programa.");
